# netlify.toml — valid TOML, no { } anywhere

[build]
  command = "npm run build"
  # Do NOT set "publish" for Next.js when using the Netlify Next plugin.

[build.environment]
  # Pick ONE Node version strategy and keep it consistent with your repo/Netlify UI.
  NODE_VERSION = "18"

[[plugins]]
  package = "@netlify/plugin-nextjs"
