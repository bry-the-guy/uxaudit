[build]
  command = "npm run build"
  publish = ".next"  # must NOT be the repo root

[build.environment]
  NODE_VERSION = "18"
  NEXT_DISABLE_ESLINT = "true"

[[plugins]]
  package = "@netlify/plugin-nextjs"
